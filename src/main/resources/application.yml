server:
  port: 8080
  address: 0.0.0.0

spring:
  application:
    name: luxurya-prodotti-api

  # --- CONFIGURAZIONE MYSQL ---
  datasource:
    url: jdbc:postgresql://16.16.192.42:5432/mydb
    username: postgre
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

    show-sql: true

    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # --- CONFIGURAZIONE MONGODB ---
  data:
    mongodb:
      uri: mongodb://execus:execus123@13.51.33.27:27017/worldall?authSource=admin
      # in alternativa puoi separare host/port/db:
      # host: localhost
      # port: 27017
      database: worldall
    redis:
      host: 16.16.192.42
      port: 6379
      password: ""     # lascia vuoto se Redis non richiede autenticazione
      timeout: 6000    # millisecondi

  # --- LOGGING ---
  logging:
    level:
      root: INFO
      org.springframework.web: INFO
      org.hibernate.SQL: DEBUG
      org.mongodb.driver: WARN

# --- PROFILI (opzionale) ---
# puoi definire profili separati per dev/prod se vuoi
# spring:
#   profiles:
#     active: dev

app:
  jwt:
    secret: c7a1b2d3e4f56789a0b1c2d3e4f56789c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5
    expiration-ms: 8640000000

execus:
  execusbiurl: http://13.51.33.27:8080